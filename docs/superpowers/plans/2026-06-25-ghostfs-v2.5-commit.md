# ghostfs v2.5 — Atomowy commit + crash sweep: plan

> REQUIRED: subagent-driven-development. BRAMKA: crash-sweep `gh2_fsck==0` dla KAŻDEGO N +
> stan stary-albo-nowy (nie częściowy). Spec: `...v2.5-commit-design.md`.
> Pliki: `src/v2/gh2_super.c` (fail_after w SB write), `src/v2/gh2_fs.{h,c}` (gh2_fsck +
> formalizacja gh2_fs_commit barier), `tests/test_v2crash.c`.

## Task 1: fail_after w SB + gh2_fsck + crash sweep
- [ ] `src/v2/gh2_super.c` `sb_write_slot`: na początku dodaj `if (dev->fail_after>0){ if(--dev->fail_after==0) return -EIO; }` (jak gh_disk_write) — sweep pokrywa zapis SB.
- [ ] `gh2_fs.c` `gh2_fs_commit`: upewnij się o kolejności fsync(dev->fd) [bariera danych] →
  gh2_commit_super [SB+fsync] → gh2_txn_alloc_commit; jasne komentarze faz. (Jeśli już taka —
  potwierdź/uporządkuj.)
- [ ] `gh2_fs.c` `gh2_fsck(fs,*issues)` (+.h): walidator ze spec (sieroty, dir→inode, nlink,
  disk_block unikalny/w mapie/w granicach, size spójne). Read-only. (Wydziel z walidatora testów.)
- [ ] `tests/test_v2crash.c`: (1) sweep N=1..M — paczka ops (mkdir/create/write multi-blok/
  unlink/rename) + fail_after=N + commit + remount świeży + gh2_fsck==0 + kanarek stary-albo-nowy;
  (2) wielokrotny commit, awaria w 3-ciej paczce → stan po 2-giej; (3) idempotencja remount;
  (4) fsck wykrywa wstrzykniętą niespójność (issues>0). RUN_TEST+main, `#include <stdlib.h>`.
- [ ] `make build/test_v2crash && ./build/test_v2crash` 0 failed; ASan ręcznie; regresja
  `make test` 0 failed. Commit.

## Self-Review
Pokrycie: fail_after SB (T1); bariery commitu (T1); gh2_fsck (T1); sweep stary-albo-nowy +
multi-commit + idempotencja + fsck-negatywny (T1). Ryzyka: (1) sweep MUSI dać fsck==0 dla
KAŻDEGO N (atomowość — bramka); (2) SB pisany OSTATNI po fsync danych (kolejność); (3) kanarek
dowodzi braku stanu częściowego; (4) fsck faktycznie wykrywa (test negatywny); (5) fail_after w
SB write nie psuje normalnej pracy (tylko gdy ustawione).
