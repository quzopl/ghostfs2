# ghostfs v2.8 — Samonaprawa: plan

> REQUIRED: subagent-driven-development. BRAMKA: read-repair (zepsuta kopia→odczyt OK z dup+
> naprawa), mark-sweep/refcount liczą OBIE kopie, regresja DUP-off (B-drzewo CoW nietknięte),
> ASan. Spec: `...v2.8-selfheal-design.md`. Pliki: `src/v2/gh2_btree.{c,h}` (node_write/read/
> walk_node), `src/v2/gh2_space.c` (vtable dup_meta), `src/v2/gh2_fs.{c,h}` (flaga DUP, format),
> `src/v2/gh2_vfs`, `src/cli.c`, `tests/test_v2heal.c`.

## Task 1: DUP write + walk dup + read-repair + flaga + testy
- [ ] `gh2_alloc` (gh2_btree.h): dodaj `int dup_meta`. `gh2_txn_alloc_vtable` (gh2_space.c) ustawia
  z flagi (np. `gh2_txn_alloc.dup_meta`, ustawiane przez gh2_fs z SB flagi GH2_SB_DUP_META).
- [ ] `gh2_node_write`: jeśli `a->dup_meta` → alokuj 2. blok, `gh_disk_write` do obu, `out_bptr->
  dup_block=blk2`; błąd → zwolnij oba. (off → jak dotąd.)
- [ ] `walk_node`: po `cb(node->block)` dodaj `if (node->dup_block) cb(node->dup_block)` (przed
  rekurencją do dzieci). → mark-sweep/refcount liczą obie kopie.
- [ ] `gh2_node_read`: przy złej sumie block + dobrej dup → po skopiowaniu dobrych danych
  best-effort `gh_disk_write(dev, bptr->block, buf)` (ignoruj błąd). Oba złe → -EIO (jest).
- [ ] `gh2_fs_format`: parametr/flaga DUP → ustaw GH2_SB_DUP_META w sb; mount → fs->dup_meta;
  vtable dup_meta z tego. CLI `format2 --dup` / fasada.
- [ ] `tests/test_v2heal.c`: format DUP → węzły 2 kopie identyczne; read-repair (zepsuj block→
  odczyt OK+naprawa; zepsuj dup→OK; oba→EIO); mark-sweep/refcount liczą obie (zajęte=2×węzły+...);
  CoW zwalnia obie; snapshot dzieli obie (refcount==2 block+dup); realistyczna samonaprawa (losowy
  bitrot 1 kopii→cały FS czytelny, fsck==0); crash+DUP fsck==0. +RUN_TEST/main/stdlib.h.
- [ ] `make build/test_v2heal && ./build/test_v2heal` 0 failed; ASan; **regresja `make test` 0
  failed (DUP off = bez zmian; B-drzewo CoW nietknięte)**; CLI format2 --dup działa. Commit.

## Self-Review
Pokrycie: DUP write (node_write), walk dup (walk_node), read-repair (node_read), flaga+format+CLI;
testy read-repair/mark-sweep/refcount/snapshot/realistic/crash/regresja. Ryzyka: (1) walk_node MUSI
liczyć dup (inaczej alloc nadpisze 2. kopię → korupcja) = bramka mark-sweep; (2) read-repair
best-effort nie psuje (zła naprawa → wciąż dobre dane z dup); (3) DUP off = zero regresji (block-
math testów); (4) refcount/snapshot/CoW obejmują obie kopie (cow_free już, refmap przez walk_node);
(5) B-drzewo split/merge/insert/delete NIETKNIĘTE (tylko I/O węzła).
