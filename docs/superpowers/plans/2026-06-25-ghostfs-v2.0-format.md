# ghostfs v2.0 — Format & superblok: plan

## Task 1: format on-disk + ping-pong superblok
**Files:** Create `src/v2/gh2_format.h`, `src/v2/gh2_super.h`, `src/v2/gh2_super.c`,
`tests/test_v2super.c`; Modify `Makefile`.

- [ ] Step 1: `Makefile` — `CFLAGS += -Isrc`; `CORE += $(wildcard src/v2/*.c)`.
- [ ] Step 2: `gh2_format.h` — GH2_MAGIC, GH2_BLOCK_SIZE, gh2_bptr, gh2_superblock, flagi
  (GH2_SB_ENCRYPTED=1, GH2_SB_DUP_META=2, GH2_SB_COMPRESS=4). `_Static_assert(sizeof(sb)<=4096)`.
- [ ] Step 3: `gh2_super.c` — gh2_sb_csum, gh2_format (oba sloty, gen=1, next_free=2),
  gh2_mount (najwyższa ważna), gh2_commit_super (gen++, slot gen&1, fsync). Raw pread/pwrite.
- [ ] Step 4: `test_v2super.c` — testy 1–5 ze spec (format/mount, commit ping-pong, CRASH
  fallback, csum, oba zepsute→-EINVAL) + RUN_TEST + main.
- [ ] Step 5: `make clean && make test && make test-asan` — 0 failed (v2 + regresja v1 A–R).
- [ ] Step 6: commit.
