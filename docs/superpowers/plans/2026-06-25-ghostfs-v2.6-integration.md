# ghostfs v2.6 — Integracja FUSE/CLI: plan

> REQUIRED: subagent-driven-development. BRAMKA: v2 montowalny przez FUSE (round-trip, edycja,
> kill-9+recover→fsck, szyfrowanie) ORAZ v1 integracja nadal zielona. Spec:
> `...v2.6-integration-design.md`. Pliki: `src/v2/gh2_vfs.{h,c}`, `src/fuse_main.c`, `src/cli.c`,
> `tests/test_v2vfs.c`, `tests/integration_v2.sh`.

## Task 1: Fasada gfs (gh2_vfs)
- [ ] `src/v2/gh2_vfs.{h,c}`: struct gfs_attr, struct gfs; gfs_detect (magic bloku 0);
  gfs_mount/unmount (v1: gh_fs_mount[_key]; v2: gh_dev_open+gh2_fs_mount, klucz przez crypto);
  wszystkie gfs_* (getattr→gfs_attr, create/mkdir/mknod/symlink/readlink/link/unlink/rmdir/
  rename/chmod/chown/utimens/truncate/read/write/readdir/statfs/sync/fsck; xattr v1→core,
  v2→-ENOTSUP). Przełączanie po version.
- [ ] `tests/test_v2vfs.c`: ten sam zestaw ops przez fasadę na kontenerze v1 i v2 (format obu,
  create/mkdir/write/read/getattr/readdir/unlink/sync/remount/fsck); ASan. + stdlib.h.
- [ ] `make build/test_v2vfs && ./build/test_v2vfs` 0 failed; ASan; commit.

## Task 2: Okablowanie FUSE + CLI + integracja
- [ ] `src/fuse_main.c`: g_fs→struct gfs; gfs_mount auto-detect (+prompt hasła); handlery gf_*
  wołają gfs_*; getattr gfs_attr→stat; gf_fsync/unmount→gfs_sync; flush leniwy; xattr v2→ENOTSUP.
  Zachowaj flock/rwlock/strojenie I/O.
- [ ] `src/cli.c`: komendy→gfs_*; cli_mount→gfs_mount; format `--v2` lub `format2` (gh2_fs_format);
  mutujące komendy → gfs_sync przed unmount.
- [ ] `tests/integration_v2.sh`: format2+mount FUSE → round-trip/edycja/mkdir/ls/rm/mv/symlink/
  hardlink/df/chmod/mknod/truncate/kill-9+recover→fsck/szyfrowanie v2. CLI v2: format2/put/get/
  ls/stat/df/fsck/rm.
- [ ] `make cli fuse && ./tests/integration_v2.sh` WSZYSTKIE PRZESZŁY; `./tests/integration.sh`
  (v1) WSZYSTKIE PRZESZŁY (regresja); `make test` 0 failed. Commit.

## Self-Review
Pokrycie: fasada wszystkich ops (T1); FUSE+CLI okablowanie + format v2 (T2); integracja v2 FUSE
+ CLI + regresja v1 (T2). Ryzyka: (1) v2 montowalny i poprawny przez FUSE = bramka (round-trip/
edycja/kill-9); (2) v1 NIETKNIĘTE (regresja integration.sh); (3) trwałość v2 przez gfs_sync na
fsync/unmount; (4) szyfrowanie v2 przez fasadę (crypto na dev2); (5) xattr v2→ENOTSUP nie wywala
narzędzi.
