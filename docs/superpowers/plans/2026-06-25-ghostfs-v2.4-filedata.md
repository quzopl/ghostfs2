# ghostfs v2.4 — Dane plików: plan

> REQUIRED: subagent-driven-development. Bramki: persystencja danych po remount, mark-sweep
> oznacza bloki danych (mapa==rzeczywista), wyciek bloków=0, csum danych→EIO, ASan. Spec:
> `docs/superpowers/specs/2026-06-25-ghostfs-v2.4-filedata-design.md`. Pliki: `src/v2/gh2_fs.{h,c}`,
> mark-sweep itemów w `gh2_space.c`/`gh2_btree.c`, `tests/test_v2fs.c`.

## Task 1: ekstenty (write/read/truncate/unlink danych) + mark-sweep bloków danych
- [ ] `struct gh2_extent` w gh2_fs.h; stała GH2_EXTENT_DATA już jest (=3).
- [ ] mark-sweep rozszerzenie: walk itemów leaf (dodaj `gh2_btree_walk_items(dev,root,cb_item)`
  LUB rozszerz build_from_tree by przy każdym leaf-itemie EXTENT_DATA oznaczyć disk_block+dup).
  `gh2_space_build_from_tree` oznacza węzły (jak dotąd) + bloki danych ekstentów.
- [ ] `gh2_fs.c`: helper read_block/write_block (CoW data block, csum, gh_disk_*); 
  `gh2_fs_write`/`gh2_fs_read` (pętla bloków, RMW częściowych, size/mtime, atomowość per-op na
  lokalnym root z savepoint/rollback); `gh2_fs_truncate` (skróć: free ekstenty≥size + ogon;
  rozszerz: size); `gh2_inode_free_extents` + podłącz do unlink (free bloków danych przy
  usunięciu i-węzła pliku).
- [ ] Test (rozszerz test_v2fs.c): round-trip różne rozmiary/offsety/dziury; nadpisanie CoW
  (wyciek=0); RMW; truncate skróć/rozszerz/zero; unlink z danymi→bloki zwolnione; persystencja
  remount (dane + mark-sweep bloków danych); csum→EIO; duży plik 4MB; spójność fsck (bloki
  danych w mapie, brak współdzielenia, size spójne). 
- [ ] `make build/test_v2fs && ./build/test_v2fs` 0 failed; ASan; regresja `make test` 0 failed. Commit.

## Self-Review
Pokrycie: ekstent item + read/write/truncate/unlink danych (T1); mark-sweep bloków danych (T1);
persystencja + csum + fsck + wyciek (T1). Ryzyka: (1) mark-sweep MUSI oznaczać bloki danych
(inaczej alokacja nadpisze żywe dane — bramka mapa==mark-sweep po write); (2) CoW nadpisania
(stary blok defer-free, nie od razu — atomowość); (3) wyciek bloków danych przy unlink/truncate;
(4) RMW częściowych bloków + dziury=zera; (5) atomowość per-op obejmuje write wielu bloków.
