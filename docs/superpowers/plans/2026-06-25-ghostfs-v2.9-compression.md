# ghostfs v2.9 — Kompresja: plan (2 zadania)

> REQUIRED: subagent-driven-development. BRAMKA: oszczędność miejsca (ściśliwe→mniej bloków),
> round-trip bajt-exact, mark-sweep liczy bloki chunków, regresja (kontener bez --compress = v2.4
> nietknięte), ASan. Spec: `...v2.9-compression-design.md`. zlib `-lz`.

## Task 1: chunk extent + zlib + read/write (kontener --compress) + mark-sweep
**Files:** `src/v2/gh2_fs.{h,c}`, `src/v2/gh2_format.h`, `Makefile` (-lz); Test `tests/test_v2comp.c`.
- [ ] `Makefile`: `LDLIBS += -lz`. GH2_EXTENT_COMP=6, GH2_COMP_CHUNK=8, GH2_SB_COMPRESS w format.h.
  `struct gh2_cext_hdr` w gh2_fs.h.
- [ ] enkod/dekod chunk extentu (hdr + blocks[]); `fs->compress` z SB flagi przy mount; format flaga.
- [ ] `chunk_read(fs,root,ino,chunk_off,*rawbuf,*raw_len)` (czytaj blocks, csum, zlib uncompress);
  `chunk_write(fs,&root,ino,chunk_off,rawbuf,raw_len)` (zlib compress2, fallback raw, alloc nblocks,
  CoW stare→defer_dec, item). `gh2_fs_read/write` rozgałęź: `fs->compress` → chunk path (pętla po
  chunkach, RMW częściowych), else → istniejąca per-blok (NIETKNIĘTA).
- [ ] `mark_extent_cb` (mark-sweep): GH2_EXTENT_COMP → ref_inc każdy blok blocks[]; GH2_EXTENT_DATA
  bez zmian. snapshot/refcount/refmap obejmują (przez walk extents istniejący).
- [ ] Test (test_v2comp.c): oszczędność (ściśliwe 1MB→<<bloków; losowe→raw); round-trip różne
  rozmiary/offsety/dziury/granice chunków bajt-exact; RMW sub-chunk; mark-sweep liczy bloki chunków
  (refcount==mark-sweep, wyciek=0); persystencja remount. RUN_TEST.
- [ ] `make build/test_v2comp && ./build/test_v2comp` 0 failed; ASan; regresja `make test` 0 failed
  (per-blok nietknięte). Commit.

## Task 2: truncate/unlink/snapshot integracja + CLI + skala
**Files:** `src/v2/gh2_fs.{h,c}`, `src/v2/gh2_vfs`, `src/cli.c`; Test `tests/test_v2comp.c`, integration_v2.sh.
- [ ] `gh2_fs_truncate` chunk path (skróć: free chunki≥align(size) + częściowy ogon RMW; rozszerz: size);
  `gh2_inode_free_extents` chunk path (free wszystkie bloki chunków). 
- [ ] CLI `format2 --compress` (+kombinacja --dup); fasada gfs. Integracja: put dużego ściśliwego pliku
  na --compress → df pokazuje oszczędność; get round-trip; fsck.
- [ ] Test: truncate (skróć/zero/rozszerz, wyciek=0); unlink z danymi (free); snapshot+kompresja
  (współdzielenie bloków chunków, izolacja, delete wyłącznych); crash-sweep z --compress (fsck==0);
  duży plik 4MB ściśliwy/mieszany; integracja CLI. 
- [ ] make + ASan + regresja + integration_v2.sh. Commit.

## Self-Review
Pokrycie: chunk format+zlib+read/write (T1); mark-sweep chunków (T1); truncate/unlink/snapshot/CLI/
crash (T2); regresja per-blok. Ryzyka: (1) mark-sweep MUSI liczyć bloki chunków (inaczej nadpisanie
żywych danych) = bramka; (2) round-trip bajt-exact (kompresja+szyfrowanie+RMW); (3) regresja: kontener
bez --compress nietknięty; (4) oszczędność realna (ściśliwe→mniej bloków); (5) fallback raw dla
nieściśliwych (brak strat); (6) snapshot/crash z chunkami.
