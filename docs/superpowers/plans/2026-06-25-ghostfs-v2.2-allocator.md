# ghostfs v2.2 — Alokator: plan

> REQUIRED: superpowers:subagent-driven-development. Bramka: spójność mark-sweep (mapa
> odbudowana z drzewa == mapa użyta) + deferred-free/abort + ASan + brak wycieku bloków.
> Pliki: `src/v2/gh2_space.{h,c}`, `gh2_btree_walk_nodes` w `gh2_btree.{c,h}`, `tests/test_v2space.c`.

## Task 1: mapa wolnej przestrzeni + alokator transakcyjny + mark-sweep
**Files:** Create `src/v2/gh2_space.h`, `src/v2/gh2_space.c`, `tests/test_v2space.c`;
Modify `src/v2/gh2_btree.h`, `src/v2/gh2_btree.c` (dodaj `gh2_btree_walk_nodes`).

- [ ] Step 1: `gh2_btree_walk_nodes(dev, root, cb, ctx)` w gh2_btree.c — DFS po wszystkich
  węzłach (internal+leaf), woła `cb(block, ctx)` dla bptr.block KAŻDEGO węzła (od korzenia w
  dół). Pomija bptr.block==0. (Czyta węzły przez gh2_node_read; zwraca błąd czytania.) Deklaracja w .h.
- [ ] Step 2: `gh2_space.h` — struct gh2_space, gh2_txn_alloc, deklaracje API ze spec
  (init/destroy/is_used/set/alloc_one; txn_alloc_init/destroy/vtable/commit/abort;
  space_build_from_tree). `gh2_alloc` z v2.1 (gh2_btree.h) jest reuzywane.
- [ ] Step 3: `gh2_space.c`:
  - bitmapa (bity, nblocks, hint, nfree); init oznacza bloki GH2_SB_SLOT_A/B (0,1) zajęte,
    resztę [GH2_DATA_START..nblocks) wolne, nfree liczone; set/is_used; alloc_one (first-fit
    od hint, zawijanie; -ENOSPC gdy nfree==0/brak).
  - `gh2_txn_alloc`: alloc = alloc_one + push txn_alloced (oom przy ENOMEM realloc → zwróć
    błąd z alloc, by mutacja drzewa się wycofała); free = push defer_free (NIE zwalnia);
    vtable zwraca {alloc_fn, free_fn, ctx=ta}; commit = dla każdego defer_free → space_set
    wolny, wyczyść obie listy; abort = dla każdego txn_alloced → space_set wolny, wyczyść listy.
  - `gh2_space_build_from_tree` = init mapy (jeśli trzeba) + walk_nodes(cb oznacza zajęty).
- [ ] Step 4: `tests/test_v2space.c` (+`#include <stdlib.h>`): testy 1–5 ze spec — mapa
  podstawy; deferred-free (zajęte do commitu) + abort (przywraca mapę); mark-sweep spójność
  (drzewo realnym alokatorem → build_from_tree świeżej mapy == zajęte przez drzewo);
  integracja insert/delete dużo + commit po każdym, mapa spójna, brak double-alloc/wycieku;
  RUN_TEST + main.
- [ ] Step 5: `make build/test_v2space && ./build/test_v2space` 0 failed; ASan ręcznie
  (`-Isrc -Itests -fsanitize=address,undefined`, źródła + gh2_space.c gh2_btree.c gh2_super.c)
  0 failed; regresja `make test` 0 failed.
- [ ] Step 6: commit.

## Self-Review
Pokrycie spec: space map + alloc_one ✓; txn alloc deferred-free/commit/abort ✓; walk_nodes +
build_from_tree (mark-sweep) ✓; testy spójności/abort/integracja ✓. Ryzyka: (1) mark-sweep
spójność = bramka (mapa z drzewa == użyta); (2) deferred-free poprawność (stare bloki zajęte do
commitu — inaczej żywe drzewo wskazuje wolny blok); (3) abort przywraca mapę (brak wycieku po
nieudanej mutacji); (4) OOM w alloc → mutacja B-drzewa musi się wycofać czysto.
