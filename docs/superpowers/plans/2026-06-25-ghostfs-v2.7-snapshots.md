# ghostfs v2.7 — Snapshoty: plan (4 zadania)

> REQUIRED: subagent-driven-development. BRAMKA każdego zadania: refcount==mark-sweep
> (gh2_refmap_build_from_roots), brak premature-free/wycieku, ASan, regresja v2.0-2.6 (refcount≡1
> → zachowanie identyczne). Spec: `...v2.7-snapshots-design.md`. Eager-refcount: B-drzewo NIETKNIĘTE.

## Task 1: Refcounty (free→dec) + refmap mark-sweep
**Files:** Modify `src/v2/gh2_space.{h,c}`; Test `tests/test_v2space.c`.
- [ ] Refcount store w gh2_space: pełna tablica `uint16_t *refs` per blok (prościej; clamp/saturate
  na max) LUB sparse — wybierz pełną. `gh2_ref_get/inc(block)`, `gh2_ref_dec(block)` (przy 0 →
  space_set wolny). Init: refcount 0 wszędzie poza SB.
- [ ] `gh2_txn_alloc`: alloc → blok refcount 1 (used); free vtable → DEFER dec (przy commit: dec,
  free przy 0); dodaj defer-inc (snapshot). savepoint/rollback obejmuje refcount-deltas op
  (abort: cofnij alloc→0, porzuć defer dec/inc tej op).
- [ ] `gh2_refmap_build_from_roots(dev, &space, root_tree_or_single_root)` — na razie z JEDNEGO
  fs_root (drzewo korzeni w Task 2): walk węzłów + bloków danych, ZLICZAJ referencje → refs[];
  used==(rc>0). (Rozszerza gh2_space_build_from_tree o liczenie zamiast tylko set.)
- [ ] Test: ref inc/dec/get; dec do 0 zwalnia; alloc=rc1; mark-sweep liczy poprawnie (zbuduj
  drzewo, refmap_build → wszystkie refs==1 dla 1 korzenia); **regresja: bez współdzielenia
  zachowanie jak dotąd** (free→dec→0→wolny == stary free). ASan.
- [ ] make build/test_v2space + ASan + regresja make test 0 failed. Commit.

## Task 2: Drzewo korzeni + subwolumeny (restrukturyzacja mount/format/commit)
**Files:** Modify `src/v2/gh2_fs.{h,c}`, `gh2_format.h`; Test `tests/test_v2fs.c`.
- [ ] GH2_ROOT_ITEM typ; struct wpisu subwolumenu (fs_root bptr, flags, name). `sb.root_tree` →
  drzewo korzeni (gh2_btree). `gh2_fs_format`: utwórz drzewo korzeni z 1 wpisem (subvol 1 →
  pusty fs-tree z rootem). `gh2_fs_mount`: wczytaj drzewo korzeni, aktywny subvol (domyślnie 1) →
  fs_root. `gh2_fs_commit`: zapisz aktywny fs_root do wpisu subwolumenu (CoW drzewa korzeni) →
  sb.root_tree=root_tree → commit_super. mark-sweep z drzewa korzeni (wszystkie subwolumeny).
- [ ] Test: istniejące test_v2fs PRZECHODZĄ (1 subwolumen przez drzewo korzeni — zachowanie
  identyczne); persystencja; refcount==mark-sweep. ASan. regresja. Commit.

## Task 3: Snapshot create + list + współdzielenie/izolacja
**Files:** Modify `src/v2/gh2_fs.{h,c}`, `gh2_vfs`, `cli`; Test `tests/test_v2snap.c`.
- [ ] `gh2_fs_snapshot(fs, name)`: nowy subvol_id, fs_root=aktywny, **inc refcount wszystkich
  bloków osiągalnych z fs_root** (walk węzły+dane), wpis do drzewa korzeni, commit. O(drzewo).
- [ ] `gh2_fs_subvol_list`. gfs_ + CLI `snapshot`/`subvol-list`. Dostęp read do snapshotu po id.
- [ ] Test (test_v2snap.c): współdzielenie (snapshot nie podwaja zajętości; refcount==2);
  izolacja (modyfikuj oryginał → snapshot niezmieniony, czytany przez jego fs_root);
  **refcount==mark-sweep** po snapshot+ops (BRAMKA); wiele snapshotów (refcount do 4). ASan.
- [ ] make + ASan + regresja. Commit.

## Task 4: Snapshot delete + crash-consistency + CLI
**Files:** Modify `src/v2/gh2_fs.{h,c}`, `gh2_vfs`, `cli`; Test `tests/test_v2snap.c`, `test_v2crash.c`.
- [ ] `gh2_fs_subvol_delete(id)`: **dec refcount wszystkich bloków** drzewa subwolumenu (free
  wyłącznych, wspólne zostają), usuń wpis, commit. CLI `subvol-del`.
- [ ] Test: delete zwalnia tylko bloki wyłączne (oryginał+wspólne nietknięte, fsck==0, brak
  wycieku); refcount==mark-sweep po delete; crash-sweep z operacją snapshot/delete → fsck==0,
  stan stary-albo-nowy. ASan. regresja. Commit.

## Self-Review
Pokrycie spec: refcounty+free→dec+refmap (T1); drzewo korzeni+subwolumeny (T2); snapshot create+
share/isolate (T3); delete+crash (T4). Ryzyka: (1) refcount==mark-sweep = BRAMKA każdego zadania
(premature-free→korupcja snapshotu, wyciek→zmarnowane miejsce); (2) B-drzewo NIETKNIĘTE — regresja
v2.0-2.6 (refcount≡1); (3) savepoint/rollback obejmuje refcount-deltas; (4) snapshot inc / delete
dec O(drzewo) poprawne (walk węzły+dane); (5) crash-consistency z snapshotem (stan stary-albo-nowy).
